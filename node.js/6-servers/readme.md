### Notes for servers Folder

Create package.json file

		>npm init
		
Install express, cors, body-parser, and jquery

		>npm install express --save
		>npm install cors --save
		>npm install body-parser --save
		>npm install jquery --save






